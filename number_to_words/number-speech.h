#ifndef NUMBER_SPEECH_H
#define NUMBER_SPEECH_H

#include <iostream>

std::string singleDigit(char);
std::string twoDigitsSmall( char, char);
std::string twoDigitsBig(char, char);
std::string threeDigits(char, char, char);
std::string fourDigits(char, char, char, char);
std::string fiveDigits(char, char, char, char, char);
std::string sixDigits(char, char, char, char, char, char);;
std::string suffixDet(char);

#endif